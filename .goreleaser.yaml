# GoReleaser configuration for agent-team-release
# Documentation: https://goreleaser.com

version: 2

project_name: agent-team-release

before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
  - id: atrelease
    main: ./cmd/atrelease
    binary: atrelease
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
    mod_timestamp: '{{ .CommitTimestamp }}'

archives:
  - id: atrelease
    builds:
      - atrelease
    format: tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
    format_overrides:
      - goos: windows
        format: zip
    files:
      - README.md
      - LICENSE
      - CHANGELOG.md

checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - '^ci:'
      - Merge pull request
      - Merge branch
  groups:
    - title: Features
      regexp: '^feat'
      order: 0
    - title: Bug Fixes
      regexp: '^fix'
      order: 1
    - title: Performance
      regexp: '^perf'
      order: 2
    - title: Refactoring
      regexp: '^refactor'
      order: 3
    - title: Other
      order: 999

release:
  github:
    owner: plexusone
    name: agent-team-release
  draft: false
  prerelease: auto
  mode: replace
  header: |
    ## Release {{ .Tag }}

    {{ .TagSubject }}

  footer: |
    ---

    **Full Changelog**: https://github.com/plexusone/agent-team-release/compare/{{ .PreviousTag }}...{{ .Tag }}

    ### Installation

    ```bash
    # Go install
    go install github.com/plexusone/agent-team-release/cmd/atrelease@{{ .Tag }}

    # Homebrew (macOS/Linux)
    brew tap plexusone/tap
    brew install atrelease
    ```

brews:
  - name: atrelease
    ids:
      - atrelease
    repository:
      owner: plexusone
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Formula
    homepage: https://github.com/plexusone/agent-team-release
    description: Multi-agent release preparation team for multi-language repositories
    license: MIT
    dependencies:
      - name: git
      - name: gh
    test: |
      system "#{bin}/atrelease", "version"
    install: |
      bin.install "atrelease"

announce:
  skip: true

